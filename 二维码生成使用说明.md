# 金富瑞二维码生成使用说明

二维码生成器已经放在本项目的 `qr-generator` 文件夹中。以后更换生产批号时，不需要手动修改网页代码。

## 每次生成

1. 双击 `qr-generator\双击打开二维码模拟器.lnk`。
2. 浏览器打开后，在“生产批号 / 生产日期”填写包装上实际使用的批号，例如 `FR 260902 1A`。
3. 选择二维码数量，保持“同步到本地网站数据”开启，点击“生成这一批”。
4. 在结果区域下载 Excel、CSV 和二维码 ZIP。文件会保存到 `F:\codex\模仿（金福瑞）\生产批号\批号文件夹`。
5. 先用结果中的二维码测试网址和批号，再点击“发布到 GitHub Pages”。
6. 等页面显示“线上记录已生效，可以扫码”后，再把二维码交给生产或印刷使用。

## 重要规则

- 金富瑞网站地址是 `https://yang1832-gg.github.io/JFR-product-trace/`，不要改成旧的云峰地址。
- 每批数量支持 1 到 10,000 张；编码会沿用 `805778` 前缀，并自动检查现有网站记录、历史 CSV 和本地登记表，避免重复。
- 生成时只保存到本机；只有点击“发布到 GitHub Pages”才会推送网站 JSON。
- Excel、CSV、二维码 ZIP 和 `qr-generator\data\registry.json` 是内部文件，不要上传到公开 GitHub。
- 同一个批号再次生成不会覆盖旧批次，工具会自动增加时间后缀。

## 本地入口

生成器使用 `http://127.0.0.1:4400/`。这是电脑本地管理页面，手机不能直接访问这个地址；手机扫码使用生成结果中的 GitHub Pages 网址。
